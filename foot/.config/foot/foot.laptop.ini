# Laptop variant -- larger font for the smaller / denser screen.
# Symlinked in as foot.local.ini by the ansible `foot` role, then included
# from the [main] section of foot.ini.
font=monospace:size=10
